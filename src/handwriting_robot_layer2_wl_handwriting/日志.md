# 成员2（wl）日常工作日志

本文件用于记录个人笔迹样本、字符库、缺字检测和风格扰动进展。每次工作后追加一条记录，不要删除历史内容。

## 日志模板

### 日期：YYYY-MM-DD

- 今日完成：
- 样本数量或覆盖率：
- 修改内容或提交：
- 测试结果：
- 遇到的问题：
- 需要其他成员协助：
- 明日计划：

### 日期：2026-07-15

- 今日完成：将第一阶段目标字符集和个人笔迹采集程序整合到成员2模块；新增用户样本存储、覆盖率检查、随机种子扰动、`StrokeDocument` 导出、示例与测试。
- 样本数量或覆盖率：目标字符集含 1000 个常用汉字、8 个领域补充汉字和 132 个字母/数字/符号，共 1140 个不重复字符；未提交真实用户样本。
- 修改内容或提交：新增模块公开 API、命令行导出、鼠标/数位板采集入口、资源说明和运行数据忽略规则；准备通过 Pull Request 提交。
- 测试结果：核心单元测试通过，源码编译通过，采集启动脚本和导出帮助入口验证通过。
- 遇到的问题：手机采集、少样本风格学习和未录字符生成尚未实现；随机种子只能改变已有字符样本。
- 需要其他成员协助：成员1需确认基础横向放置与正式排版模块的对接方式，以及后续 `StrokeDocument` 字段版本变更流程。
- 明日计划：用手机触控网页采集做技术验证；整理少样本风格迁移所需的标准字形骨架和训练/评估数据格式。

### 日期：2026-07-15（手机采集与统一协议）

- 今日完成：新增手机浏览器触控采集页面、局域网 HTTP 服务、六位访问令牌、断点续录和多设备统一采集协议。
- 样本数量或覆盖率：目标字符集仍为 1140 个字符；自动化测试仅使用临时合成样本，没有提交真实笔迹。
- 修改内容或提交：新增 `mobile_server.py`、`capture_protocol.py`、`web/`、手机启动脚本、采集提交示例和两份独立功能说明；手机采集作为探索功能保留，主线仍为数位板输入。
- 测试结果：协议归一化、非法数值拒绝、服务保存恢复、HTTP 鉴权和真实请求往返测试通过；原有字符库与导出测试继续通过。
- 遇到的问题：手机与笔记本必须位于同一可信局域网，Windows 防火墙可能需要允许 Python 专用网络通信；不同手机不一定提供真实压感和倾角。
- 需要其他成员协助：成员1后续只需继续调用统一 `SampleStore`/`StrokeDocument` 接口，无需处理手机传输细节。
- 明日计划：在实际手机和触控笔设备上采集小规模样本，统计断笔率、轨迹点密度和不同浏览器兼容性。

### 日期：2026-07-21（数位板用户笔迹试采准备）

- 今日完成：建立 100 个结构多样的数位板代表字符集；增加数位板强制模式、采集会话元数据、设备识别信息和采集质量报告。
- 样本数量或覆盖率：试采目标为 1 名用户 × 100 个汉字 × 1 个变体；当前只完成工具和字符集准备，尚未录入真实数位板样本。
- 修改内容或提交：新增 `style_probe_charset.json`、`collection_report.py`、一键试采/报告脚本和数位板现场采集说明。
- 测试结果：代表字符数量与去重、数位板来源强制校验、会话保存和质量报告均使用合成数据通过测试。
- 遇到的问题：当前没有连接真实数位板，压力、倾角和驱动兼容性仍需硬件现场验证。
- 需要其他成员协助：借用或采购一块支持 Windows 压感的数位板，并确认可安装官方驱动。
- 明日计划：连接真实数位板，先录入 10 个字符检查压感和轨迹，再完成 100 字第一轮试采。

### 日期：2026-07-21（100 字清洗与标准化）

- 今日完成：对 100 个真实数位板样本建立并执行非破坏性清洗流水线，包含传感器事件去重、时间归零、几何去密、轻度平滑、等弧长重采样和字形标准化。
- 样本数量或覆盖率：`user_01` 的 100/100 个字符全部处理成功，无缺失或失败样本；原始 58255 个点生成 34944 个重采样点，保留 701 个笔画及其顺序。
- 修改内容或提交：新增 `preprocessing.py`、批处理 CLI、一键脚本和清洗格式说明；生成 100 个处理样本及 1 份报告，结果只写入被 Git 忽略的 `runtime_data/`。
- 测试结果：19 项单元测试和源码编译通过；逐文件校验确认坐标有限、时间单调、笔画数不变、`glyph` 保持宽高比，原始文件处理前后组合哈希一致；抽查“一、人、中、国、器、题”未发现明显字形失真。
- 遇到的问题：删除了 23 个完全重复事件和 1822 个过密几何点；“一”的落笔前等待为 31766 毫秒，时间归零后有效书写时长为 703 毫秒。
- 需要其他成员协助：暂无。
- 明日计划：从 `dynamics`、`canvas` 和 `glyph` 三套表示中提取用户字体风格特征。

### 日期：2026-07-21（用户字体风格特征提取）

- 今日完成：实现并执行用户字体风格分析，从 `dynamics`、`canvas` 和 `glyph` 提取字面布局、速度与停顿、压力与倾角、方向分布、横竖倾斜、曲率和笔画直线度。
- 样本数量或覆盖率：`user_01` 的 100/100 个代表字符全部进入风格档案，10 个分析组均为 10/10，特征估计可信度为高，无提取失败或传感器缺失警告。
- 修改内容或提交：新增 `style_analysis.py`、命令行入口、一键脚本和独立功能说明；真实档案写入被 Git 忽略的 `runtime_data/handwriting/style_profiles/`。
- 测试结果：22 项单元测试和源码编译通过；档案数值有限、方向分布归一化、相同输入可复现，100 个字符的组合指纹和每字来源哈希均已保留。
- 遇到的问题：压力起止点会包含数位板落笔和抬笔时的零压力事件，因此生成器应优先使用整体压力和压力曲线，不能把起止点均值直接当作写字机下压力。
- 需要其他成员协助：下一阶段需要确定标准汉字有序笔画骨架的来源和授权，骨架必须保留笔顺及笔画边界。
- 明日计划：建立标准字形骨架输入格式，并实现第一版基于风格档案的确定性轨迹变换。

### 日期：2026-07-21（标准骨架风格化轨迹基线）

- 今日完成：定义标准有序笔画骨架 JSON 格式和严格校验器；实现基于用户风格档案的字面尺寸、重心、倾斜、曲线和随机种子变换，并输出 `StrokeDocument` v0.1。
- 样本数量或覆盖率：公开演示骨架仅含未录字符“永”“文”，分别保留 5 笔和 4 笔；使用 `user_01` 风格档案生成 9 笔、163 个毫米坐标轨迹点。
- 修改内容或提交：新增 `skeleton.py`、`style_generator.py`、命令行入口、Windows 一键脚本、合成风格示例、手工演示骨架及独立说明；真实生成结果写入被 Git 忽略的 `runtime_data/output/`。
- 测试结果：27 项单元测试和源码编译通过；验证坐标范围、笔顺连续、相同种子可复现、不同种子产生变化、UTF-8 BOM 请求兼容和缺少骨架时明确失败。
- 遇到的问题：采集窗口字面占比不能直接当作纸面字框占比，已增加 `glyph_occupancy` 使输出默认填充目标字框约 78%；“永”的局部形态仍受非权威手工演示骨架限制。
- 需要其他成员协助：确定并审核授权清晰、包含正确笔顺和中心线笔画边界的正式标准骨架数据源，不能直接使用普通字体轮廓替代。
- 明日计划：接入正式骨架库并建立生成轨迹的可读性、风格相似度和写字机可执行性评估流程。

### 日期：2026-07-21（Hanzi Writer Data 汉字骨架接入）

- 今日完成：审核并接入 Hanzi Writer Data 2.0.1；实现固定版本下载与 SHA-512 校验、Arphic Public License 检查、中心线坐标转换、按需字符加载和覆盖率报告。
- 样本数量或覆盖率：1000 个常用汉字和 8 个领域补充汉字全部覆盖，汉字为 1008/1008；完整 1140 字符集覆盖 1008 项，剩余 132 项均为字母、数字或符号。
- 修改内容或提交：新增 `hanzi_writer_adapter.py`、CLI、下载脚本、调用示例和独立数据源说明；第三方数据及私有覆盖报告只保存在被 Git 忽略的 `runtime_data/`。
- 测试结果：31 项单元测试和源码编译通过；1008 个汉字全部完成中心线转换校验，无无效数据；使用未采集字符“好世界”生成 20 笔、436 个轨迹点，坐标和页面边界检查通过。
- 遇到的问题：部分上游中心线笔锋超出标称 1024 画布，不能截断；已按每字真实包围盒等比例归一化并保留宽高比。数据源没有统一笔画类型标签，暂记为 `unknown`。
- 需要其他成员协助：确认项目展示和发布时是否只提供下载脚本，或需要在满足 Arphic Public License 条件后另行分发派生骨架；字母和符号仍需选择许可清晰的单线骨架来源。
- 明日计划：建立生成字形与真实 `user_01` 样本的几何相似度指标，并准备写字机小规模落笔测试文件。

### 日期：2026-07-22（生成轨迹几何相似度评估）

- 今日完成：建立生成轨迹与真实预处理样本的确定性几何比较基线，支持整体形状、逐笔形状、笔画数、宽高比和方向分布指标，并输出汇总 JSON 报告。
- 样本数量或覆盖率：工具可自动评估生成文档与处理目录中的共同字符；缺少真实参考的字符进入 `skipped`，不会混入平均分。
- 修改内容或提交：新增 `similarity.py`、命令行入口、一键脚本、公开 API、自动化测试和独立功能说明；报告默认建议写入被 Git 忽略的 `runtime_data/`。
- 测试结果：使用合成轨迹验证相同几何得到满分、报告可复现、缺少参考字符可明确跳过；完整测试结果以本次提交前实际运行结果为准。
- 遇到的问题：标准骨架和个人书写可能存在拆笔差异，逐笔分数不能单独作为质量结论；当前指标也不评价可读性和写字机动力学。
- 需要其他成员协助：后续由成员5确认设备安全参数，成员2仅准备小规模 `StrokeDocument` 测试文件。
- 明日计划：用真实 100 字共同字符生成基线报告，筛选一小组低风险字符准备写字机落笔联调。

### 日期：2026-07-22（低分字符原因诊断）

- 今日完成：在相似度报告中加入自动诊断层，将笔画拆分差异与整体形状、宽高比、方向分布和逐笔几何偏差分开，并生成按优先级排列的人工复核队列。
- 样本数量或覆盖率：真实 100 字全部重新评估；23 字存在真实样本与标准骨架笔画数差异，其中 18 字整体形状仍较接近，54 字未发现主要几何问题。
- 修改内容或提交：扩展 `similarity.py` 报告结构和公开 API，新增诊断测试并补充使用说明；私有报告仍位于 `runtime_data/handwriting/reports/`。
- 测试结果：34 项单元测试、源码编译和差异检查通过；相同输入的诊断队列可复现。
- 遇到的问题：高优先级字符为“们、计、你、进”；其中“你、进”同时存在拆笔和形状差异，“们、计”主要为笔画数一致时的几何差异。
- 需要其他成员协助：暂无；下一步先由成员2进行轨迹叠加检查和特征原因分析。
- 明日计划：为高优先级字符生成真实/生成轨迹叠加数据，判断需要调整全局风格参数还是局部部件规则。

### 日期：2026-07-22（行书与楷书差异修正）

- 今日完成：根据日常行书与规范楷书的差异修正风格分析和相似度评估；新增用户连笔、省抬笔和拆笔倾向统计，并将严格楷书评分与行书容错评分分开。
- 样本数量或覆盖率：`user_01` 的 100 字全部重新分析；约 22% 字符的实际落笔段数少于规范楷书笔画数，约 1% 多于规范笔画数。
- 修改内容或提交：扩展 `style_analysis.py`、`similarity.py` 和生成结果元数据；明确楷书骨架只作为结构底稿，当前不自动添加未经验证的连笔轨迹。
- 测试结果：35 项单元测试、源码编译和差异检查通过；严格楷书评分均值为 0.774136，行书容错评分均值为 0.816907。
- 遇到的问题：系统可以识别并容忍行书式笔画边界差异，但尚不能从统计比例推断具体的安全连接曲线。
- 需要其他成员协助：暂无；正式写字机测试前仍需成员5审核设备安全参数。
- 明日计划：提取真实样本中的笔画端点邻接关系，为字符级行书连接候选建立只读分析数据，不直接生成机器落笔路径。

### 日期：2026-07-22（行书连接候选分析）

- 今日完成：实现真实行书相邻落笔段的只读连接候选分析，综合端点距离、离笔停顿、收笔方向和起笔方向筛选等待人工复核的边界。
- 样本数量或覆盖率：`user_01` 的 100 字包含 601 个相邻落笔段边界；默认保守阈值筛出 51 个候选，候选率约 8.5%，其中 5 个为高置信候选。
- 修改内容或提交：新增 `connection_analysis.py`、命令行入口、一键脚本、公开 API、测试和独立说明；真实候选报告写入被 Git 忽略的 `runtime_data/handwriting/reports/`。
- 测试结果：37 项单元测试、源码编译和差异检查通过；真实报告确认不会生成落笔连接路径。
- 遇到的问题：高置信候选位于“室、差、家、安、江”，但当前尚未与规范楷书的语义笔画编号对齐，不能据此直接连笔。
- 需要其他成员协助：暂无；后续若进入机器落笔验证，需成员5审核安全参数。
- 明日计划：对高置信候选做小规模“实际落笔段 ↔ 规范楷书笔画”人工对齐，建立可审核标注格式。

### 日期：2026-07-23（行书笔画人工对齐审核包）

- 今日完成：建立实际落笔段与规范楷书骨架笔画的有序人工映射格式，支持一对一、一段合多笔、多段拆一笔和多对多关系；生成彩色 SVG 对照预览和严格校验器。
- 样本数量或覆盖率：为 5 个高置信候选字符“安、室、家、差、江”生成私有审核包；“家”为实际 9 段对照楷书 10 笔，其余四字两侧数量相同。
- 修改内容或提交：新增 `alignment_review.py`、命令行入口、一键脚本、公开 API、测试和独立说明；审核 JSON 与 5 张 SVG 预览写入被 Git 忽略的 `runtime_data/handwriting/alignment_reviews/user01_v1/`。
- 测试结果：39 项单元测试、源码编译和差异检查通过；真实审核包生成与校验通过，全部状态保持 `pending`。
- 遇到的问题：当前环境不能直接将 SVG 渲染为位图抽查，但 SVG 结构、坐标、笔画编号和候选虚线已完成文本检查。
- 需要其他成员协助：需要人工查看 5 张预览，填写实际落笔段到楷书笔画的对应关系并决定候选是否允许研究连笔。
- 明日计划：审核完成后提取获准连接的真实曲线形态；在审核前不修改生成器，也不产生写字机连笔路径。

### 日期：2026-07-23（免强制人工审核的个人字体档案）

- 今日完成：将人工审核调整为可选增强；实现保持笔顺的自动动态规划对齐和一键个人字体档案构建，低置信字符自动回退到明确抬笔的安全骨架。
- 样本数量或覆盖率：`user_01` 的 100/100 字均可在无人工审核条件下形成可用档案；最终 86 字获得高/中置信自动精细对齐，14 字使用安全回退并保留可选优化清单。
- 修改内容或提交：新增 `automatic_alignment.py`、`personal_font_profile.py`、两个命令行入口、一键脚本、公开 API、测试和说明；人工审核工具继续保留但不属于主线前置步骤。
- 测试结果：自动对齐先后消除了多对多等价路径和先合后拆的伪歧义；完整测试结果以本次最终运行结果为准。
- 遇到的问题：自动对齐只确定有序对应关系，仍不能从对应关系直接生成安全连笔曲线。
- 需要其他成员协助：无需人工逐字审核；如追求复杂字符更高相似度，可选择性检查 14 个低置信字符。
- 明日计划：将个人字体档案接入轨迹生成请求，使生成器自动选择精细对应或安全回退策略。

### 日期：2026-07-23（个人字体档案接入轨迹生成）

- 今日完成：轨迹生成器支持显式加载或按 `user_id` 自动发现 `personal_font_profile_v1.json`；加载时验证清单引用的风格档案和自动对齐档案 SHA-256、书写者 ID、样本指纹及数量一致性。
- 生成策略：高/中置信已采字符使用字符级字面宽高、重心、倾斜和直线度并与全局先验融合；低置信已采字符使用安全标准骨架回退；未采字符使用用户整体风格迁移。
- 安全边界：所有策略保持标准骨架的笔画数量、顺序和边界，`ligature_applied` 始终为 `false`，不把楷书端点直连，也不向写字机发送设备指令。
- 真实验证：使用 `user_01` 对“一你图好”完成私有冒烟生成，分别覆盖高置信、中置信、低置信和未采字符四种情况，共输出 22 笔，未生成连笔；请求和输出留在被 Git 忽略的 `runtime_data/`。
- 测试结果：新增清单哈希篡改、书写者不匹配、三种策略选择、确定性、笔顺保持、自动发现及旧请求兼容测试；完整测试结果以本次最终运行结果为准。
- 下一步计划：对个人字体生成结果运行批量几何相似度评估，比较接入字符级特征前后的变化，并准备少量写字机安全联调轨迹。

### 日期：2026-07-23（个人字体字符级增强批量评估）

- 今日完成：实现多随机种子的个人字体对照评估工具，自动比较“用户整体风格基线”和“字符级特征增强”，逐字统计严格楷书分数、行书容错分数及各几何指标差值。
- 自动质量门：检查全部字符是否完成评估、笔画边界是否保持、是否意外生成连笔、总体均值、退化比例和严重退化数量；失败时只阻止全量启用字符级增强，不阻塞个人字体库。
- 真实结果：`user_01` 100 字在种子 0、17、42 下，基线行书容错均值为 0.816387，候选为 0.819829；37 字改善、32 字稳定、31 字退化，其中 6 字严重退化，全量启用质量门失败。
- 自动部署建议：55 字保留字符级增强，45 字自动使用安全回退，投影行书容错均值为 0.828209，相对基线提高 0.011822；人工审核必需数为 0。
- 安全验证：两路生成的笔画数量、顺序和边界完全一致，没有生成连笔；真实报告保存在被 Git 忽略的 `runtime_data/handwriting/reports/`。
- 下一步计划：把自动评估通过/回退名单写成带哈希的部署策略产物，并由生成器直接加载，使 45 个退化或低置信字符在实际生成时自动回退。

### 日期：2026-07-23（个人字体逐字部署策略）

- 今日完成：实现 `personal_font_deployment_v1.json` 构建与严格加载，引用并校验个人字体清单和批量评估报告 SHA-256、用户 ID、字符覆盖、对齐置信度和逐字推荐策略。
- 生成器接入：默认请求会优先自动发现部署策略；评估通过的字符启用字符级特征，退化字符即使对齐置信度为高也强制安全回退，未采字符继续使用用户整体风格。
- 真实结果：为 `user_01` 生成 55 字增强、45 字回退的私有部署策略；使用“一三图好”验证了增强、高置信强制回退、低置信回退和未采字符四种路径。
- 安全验证：部署策略及其引用产物任一哈希不一致时拒绝加载；实际生成未改变笔画数量、笔顺或边界，未生成连笔，也不控制写字机。
- 下一步计划：使用最终部署策略生成少量写字机联调 `StrokeDocument`，覆盖增强、回退和未采字符，并交给设备成员做机械边界与速度审核。

### 日期：2026-07-23（写字机联调轨迹交付包）

- 今日完成：实现基于最终个人字体部署策略的联调轨迹包生成器，自动选择字符级增强、高/中置信评估回退、低置信回退、未采字符和混合冒烟五种场景。
- 软件预检：校验 `StrokeDocument` v0.1、有限坐标、A4 页面范围、连续笔顺、字符映射、明确抬笔段、逐字部署策略、运动提示覆盖及相邻点距；不生成设备命令。
- 真实结果：`user_01` 生成“他一”“二火”“图家”“好世”“他二图好”5 份私有轨迹，共 62 笔、1557 个点，最大相邻点距约 0.205 mm，全部软件预检通过，连笔数为 0。
- 安全边界：清单明确设置 `machine_ready=false`，只达到 `ready_for_layout_and_device_review`；成员1仍需完成正式页面区域变换，成员5仍需完成机械标定、速度/加速度、笔压、空跑和急停审核。
- 下一步计划：把私有交付包路径和审核清单交给成员1/5；收到设备标定参数后，仅在成员2模块内补充相应的轨迹采样密度或尺寸兼容检查，不接管设备控制。

